#include<iostream>
using namespace std;

void solve(int nums[],int output,int index ,int &ans )
{
    // base case
    if(index>=nums.size());
    ans.push(output);
    return;
}
int main(){
   int arr[3] = {1,2,3};
   int output;
   int index;
   int ans;
}